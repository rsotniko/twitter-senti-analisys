class Display(object):

    def __init__(self, saver_data):
        self.data = saver_data
        # We need a parser here, to parse our result
        # self.frame1 = Parse(saver_data)
        # self.frame2 = Parse(saver_data)

    # main method to display
    @staticmethod
    def display(param):
        return 0
